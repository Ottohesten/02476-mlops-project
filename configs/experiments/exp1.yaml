# @package _global_
dataset_path: "${hydra:runtime.cwd}/data"
batch_size: 100
x_dim: 784
hidden_dim: 400
latent_dim: 20
lr: 1e-3
num_epochs: 20
seed: 42
optimizer:
  _target_: torch.optim.Adam
  lr: 1e-3
  betas: [0.9, 0.999]
  eps: 1e-8
  weight_decay: 0
